{
  "name": "netlify-upstream-proxy",
  "version": "2.0.0",
  "description": "Enhanced Netlify Edge Function for proxying requests to upstream services",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Build completed - static assets ready'"
  },
  "devDependencies": {
    "netlify-cli": "latest"
  }
}
